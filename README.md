# TripTap — Flutter class project

Flutter source and `pubspec.yaml` are in **`Project_Code/`**. Open that directory as the app root, then run `flutter pub get` and `flutter run`.

This repository also includes the written project report as `MarkoPadilla_TripTap_Report.pdf`.
